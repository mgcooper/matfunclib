function symbols = defaultmarkers

symbols = {'*','+','.','<','>','^','v','x','diamond','hexagon','pentagram',...
   'square','o','|'};

% I don't include the marker '_' b/c it's indistinguishable from a line when
% used as a line-marker